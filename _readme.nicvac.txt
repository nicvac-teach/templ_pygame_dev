Lo scopo è lo stesso di templ_pygame.
Qui però, per cercare di evitare la prebuild,
si parte dal template blank di codespace e si eseguono gli script per installare un ambiente grafico con Codespace (VNC web).
L'installazione di flux e Vnc comunque richiede del tempo...
Con la prebuild di templ_pygame si va più rapidi, questo è un compromesso che non richiede prebuild.
